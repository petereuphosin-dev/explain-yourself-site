# Hero Media Assets

Add the launch poster image here as:

- `hero-poster.png`

Optional:

- `hero.mp4`

If `hero.mp4` is missing, the home page modal will show a clean placeholder message and keep using the poster artwork.
